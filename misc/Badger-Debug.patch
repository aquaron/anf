--- /tmp/patch.pm
+++ /usr/local/share/perl5/site_perl/Badger/Debug.pm
@@ -106,7 +106,7 @@
         if defined ${ $target.PKG.DEBUG };
     
     $self->debug("$symbol option setting $target DEBUG to $value\n") if $DEBUG;
-    *{ $target.PKG.DEBUG } = sub () { $value };
+    *{ $target.PKG.DEBUG } = sub () { return $value };
 }
 
 
